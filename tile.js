/**
 * @author Max Kerscher-Santelli
 */
/*
var speed = 5;

var Tile = function(x, y){
	console.log("new tile x: " + x + " y: " + y);
	this.sp = new PIXI.Sprite(PIXI.Texture.fromImage("images/Untitled-1.jpg"));
	this.sp.position.x = x;
	this.sp.position.y = y;
	stage.addChild(this.sp);
};

Tile.prototype.test = function(){
	console.log("Tile.test");
};

Tile.prototype.update = function(up, down, left, right){
	console.log("tile update");
	if(up){
		this.sp.position.y -= 5;
	}
	if(down){
		this.sp.position.y += 5;
	}
	if(right){
		this.sp.position.x += 5;
	}
	if(left){
		this.sp.position.x -= 5;
	}
	
	if(this.sp.position.x < -70 || this.sp.position.x > 700 || this.sp.position.y < -70 || this.sp.position.y > 700){
		this.sp.visible = false;
	}else{
		this.sp.visible = true;
	}
};*/
